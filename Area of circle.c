//Area of circle

#include <stdio.h>

float main() {
  float r,area;
  printf("Enter the value of radius");
  scanf("%f",&r);
  area=3.14*r*r;
  printf("Area of circle is %f",area);
    
    return(0);
}
